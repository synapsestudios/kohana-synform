<?php defined('SYSPATH') OR die('No direct access allowed.');

return array(
	/**
	 * Setting group chosen by default when creating the Synform object
	 */
	'default' => array(
		/**
		 * Directory in views/synform/themes to use for this form
		 * Each element must have a view inside this directory
		 */
		'theme' => 'default',
	),
);
